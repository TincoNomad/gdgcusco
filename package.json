{
  "name": "gdg_cusco",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:deploy": "astro build && rm -rf dist-final && mkdir -p dist-final/devfest2026 && cp -r dist/* dist-final/devfest2026/ && rm -rf dist && mv dist-final dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "allowScripts": {
    "esbuild": true
  }
}